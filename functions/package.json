{
  "name": "creatorcircle-functions",
  "description": "Cloud Functions (2nd gen) for CreatorCircle — background web push delivery",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.4.0",
    "web-push": "^3.6.7"
  }
}
